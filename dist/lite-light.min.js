let P = !1;
const z = 20, g = /* @__PURE__ */ new Map();
let l = {
  startX: 0,
  startY: 0,
  initialDistance: 0,
  isZooming: !1,
  lastCenterX: 0,
  lastCenterY: 0
}, t = {
  scale: 1,
  x: 0,
  y: 0,
  initialScale: 1,
  initialX: 0,
  initialY: 0
};
const K = 0.01, Y = 1, R = 5;
let X = !1;
const k = typeof window < "u" ? window.matchMedia("(max-width: 768px)") : { matches: !1 };
function E(e) {
  if (g.has(e)) {
    const i = g.get(e);
    return g.delete(e), g.set(e, i), i;
  }
  if (g.size >= z) {
    const i = g.keys().next().value;
    g.delete(i);
  }
  const o = new Image();
  return o.src = e, g.set(e, o), o;
}
function $(e) {
  typeof requestIdleCallback == "function" ? requestIdleCallback(e, { timeout: 2e3 }) : setTimeout(e, 0);
}
let M = 0;
function j() {
  M = window.scrollY, document.body.style.position = "fixed", document.body.style.top = `-${M}px`, document.body.style.width = "100%", document.body.style.overflowY = "scroll";
}
function G() {
  document.body.style.position = "", document.body.style.top = "", document.body.style.width = "", document.body.style.overflowY = "", window.scrollTo({
    top: M,
    left: 0,
    behavior: "instant"
  });
}
function B(e) {
  if (e.length < 2) return 0;
  const o = e[0], i = e[1];
  return Math.sqrt(
    Math.pow(i.screenX - o.screenX, 2) + Math.pow(i.screenY - o.screenY, 2)
  );
}
function N(e) {
  if (e.length < 2) return { x: e[0].screenX, y: e[0].screenY };
  const o = e[0], i = e[1];
  return {
    x: (o.screenX + i.screenX) / 2,
    y: (o.screenY + i.screenY) / 2
  };
}
function T(e) {
  e.style.transform = `scale(${t.scale}) translate(${t.x}px, ${t.y}px)`;
}
function _(e) {
  X || (X = !0, requestAnimationFrame(() => {
    T(e), X = !1;
  }));
}
function x(e, o = !1) {
  const i = !L(t.scale) || t.x !== 0 || t.y !== 0;
  t.scale = 1, t.x = 0, t.y = 0, o && i ? (e.style.transition = "transform 0.2s ease", T(e), e.addEventListener("transitionend", () => {
    e.style.transition = "";
  }, { once: !0 })) : (e.style.transition = "", T(e));
}
function L(e) {
  return Math.abs(e - 1) < K;
}
function Q(e = {}) {
  if (P) return;
  P = !0;
  const o = {
    imageSelector: e.imageSelector || "img[data-lightbox]",
    imageUrlAttribute: e.imageUrlAttribute || "data-lightbox",
    lightboxClass: e.lightboxClass || "lite-light",
    swipeThreshold: e.swipeThreshold || 50,
    fadeAnimationDuration: e.fadeAnimationDuration || 150,
    ...e
  };
  document.querySelector(`.${o.lightboxClass}`) || J(o.lightboxClass);
  const i = document.querySelector(`.${o.lightboxClass}`), c = i.querySelector("img"), v = i.querySelector(".lite-light-prev"), b = i.querySelector(".lite-light-next"), f = i.querySelector(".lite-light-close");
  document.addEventListener("click", (w) => {
    if (w.target.tagName !== "IMG" || !w.target.hasAttribute(o.imageUrlAttribute)) return;
    const d = Array.from(document.querySelectorAll(o.imageSelector));
    let u = d.findIndex((n) => n === w.target), A = !1;
    function D(n) {
      const a = (n - 1 + d.length) % d.length, s = (n + 1) % d.length;
      E(d[a].getAttribute(o.imageUrlAttribute)), E(d[s].getAttribute(o.imageUrlAttribute));
    }
    function C(n) {
      if (A) return;
      A = !0, u = (n + d.length) % d.length;
      const a = d[u].getAttribute(o.imageUrlAttribute), s = E(a), r = () => {
        c.classList.add("lite-light-fade-out"), c.addEventListener("animationend", function p() {
          const y = () => {
            c.src = a, c.alt = d[u].alt || "", x(c), c.classList.remove("lite-light-fade-out"), c.classList.add("lite-light-fade-in"), $(() => D(u)), c.addEventListener("animationend", function() {
              c.classList.remove("lite-light-fade-in"), A = !1;
            }, { once: !0 });
          };
          s.decode ? s.decode().then(y).catch(y) : y(), c.removeEventListener("animationend", p);
        }, { once: !0 });
      };
      L(t.scale) ? r() : (x(c, !0), c.addEventListener("transitionend", r, { once: !0 }));
    }
    function h(n) {
      switch (n) {
        case "close":
          F();
          break;
        case "prev":
          C(u - 1);
          break;
        case "next":
          C(u + 1);
          break;
      }
    }
    function S(n) {
      const a = n.touches;
      if (a.length === 1) {
        const s = a[0];
        l.startX = s.screenX, l.startY = s.screenY, l.lastCenterX = s.screenX, l.lastCenterY = s.screenY, l.isZooming = !1, t.initialScale = t.scale, t.initialX = t.x, t.initialY = t.y;
      } else if (a.length === 2) {
        l.initialDistance = B(a);
        const s = N(a);
        l.lastCenterX = s.x, l.lastCenterY = s.y, t.initialScale = t.scale, t.initialX = t.x, t.initialY = t.y, l.isZooming = !0;
      }
    }
    function I(n) {
      const a = n.touches;
      if (a.length === 2) {
        const s = B(a), r = N(a);
        if (l.initialDistance > 0) {
          const p = s / l.initialDistance;
          if (t.scale = Math.max(Y, Math.min(R, t.initialScale * p)), L(t.scale))
            t.x = 0, t.y = 0;
          else {
            const y = r.x - l.lastCenterX, H = r.y - l.lastCenterY;
            t.scale > Y && (t.x = t.initialX + y / t.scale, t.y = t.initialY + H / t.scale);
          }
          _(c);
        }
        l.lastCenterX = r.x, l.lastCenterY = r.y, l.isZooming = !0;
      } else if (a.length === 1 && t.scale > Y) {
        const s = a[0], r = s.screenX - l.lastCenterX, p = s.screenY - l.lastCenterY;
        t.x += r / t.scale, t.y += p / t.scale, _(c), l.lastCenterX = s.screenX, l.lastCenterY = s.screenY, l.isZooming = !0;
      }
    }
    function Z(n) {
      if (n.changedTouches.length === 1 && !l.isZooming && n.touches.length === 0 && L(t.scale)) {
        const a = n.changedTouches[0], s = a.screenX - l.startX, r = a.screenY - l.startY;
        Math.abs(s) > Math.abs(r) && Math.abs(s) > o.swipeThreshold && (C(s > 0 ? u - 1 : u + 1), n.stopPropagation());
      }
      n.touches.length === 0 && (L(t.scale) && t.scale !== 1 && x(c, !0), l.isZooming = !1, l.initialDistance = 0);
    }
    function q(n) {
      switch (n.key) {
        case "ArrowLeft":
          h("prev"), n.preventDefault();
          break;
        case "ArrowRight":
          h("next"), n.preventDefault();
          break;
        case "Escape":
          h("close"), n.preventDefault();
          break;
        case "Enter":
        case " ":
          document.activeElement === f ? (h("close"), n.preventDefault()) : document.activeElement === v && !k.matches ? (h("prev"), n.preventDefault()) : document.activeElement === b && !k.matches && (h("next"), n.preventDefault());
          break;
      }
    }
    function U(n) {
      if (n.key !== "Tab") return;
      const a = k.matches ? [f] : [f, v, b], s = a[0], r = a[a.length - 1];
      n.shiftKey ? (document.activeElement === s || !i.contains(document.activeElement)) && (n.preventDefault(), r.focus()) : (document.activeElement === r || !i.contains(document.activeElement)) && (n.preventDefault(), s.focus());
    }
    function m(n) {
      n.stopPropagation();
      const a = n.currentTarget;
      a === f ? h("close") : a === v ? h("prev") : a === b && h("next");
    }
    function O(n) {
      n.stopPropagation(), h("close");
    }
    function F() {
      i.classList.remove("lite-light-active"), x(c, !0), G(), document.removeEventListener("keydown", q), document.removeEventListener("keydown", U), i.removeEventListener("touchstart", S), i.removeEventListener("touchmove", I), i.removeEventListener("touchend", Z), v.removeEventListener("click", m), b.removeEventListener("click", m), f.removeEventListener("click", m), i.removeEventListener("click", O);
    }
    i.classList.add("lite-light-active"), E(d[u].getAttribute(o.imageUrlAttribute)), $(() => D(u)), c.src = d[u].getAttribute(o.imageUrlAttribute), c.alt = d[u].alt || "", x(c), c.classList.add("lite-light-fade-in"), j(), f.focus(), c.addEventListener("animationend", () => {
      c.classList.remove("lite-light-fade-in");
    }, { once: !0 }), i.addEventListener("touchstart", S, { passive: !0 }), i.addEventListener("touchmove", I, { passive: !0 }), i.addEventListener("touchend", Z, { passive: !0 }), document.addEventListener("keydown", q), document.addEventListener("keydown", U), v.addEventListener("click", m), b.addEventListener("click", m), f.addEventListener("click", m), i.addEventListener("click", O);
  });
}
function J(e) {
  const o = `
    <div class="${e}" role="dialog" aria-modal="true" aria-label="Image lightbox">
      <div class="lite-light-prev lite-light-button" role="button" aria-label="Previous image" tabindex="0">
        <span class="lite-light-arrow lite-light-left"></span>
      </div>
      <img alt="" />
      <div class="lite-light-next lite-light-button" role="button" aria-label="Next image" tabindex="0">
        <span class="lite-light-arrow lite-light-right"></span>
      </div>
      <div class="lite-light-close lite-light-button" role="button" aria-label="Close lightbox" tabindex="0">
        <span class="lite-light-bar"></span>
        <span class="lite-light-bar"></span>
      </div>
    </div>
  `;
  document.body.insertAdjacentHTML("beforeend", o);
}
function V(e) {
  Q(e);
}
export {
  V as init,
  Q as initLiteLight
};
