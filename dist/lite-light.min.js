let z = !1;
const G = 20, v = /* @__PURE__ */ new Map();
let c = {
  startX: 0,
  startY: 0,
  initialDistance: 0,
  isZooming: !1,
  lastCenterX: 0,
  lastCenterY: 0
}, t = {
  scale: 1,
  x: 0,
  y: 0,
  initialScale: 1,
  initialX: 0,
  initialY: 0
};
const Q = 0.01, M = 1, J = 5;
let T = !1;
const D = typeof window < "u" ? window.matchMedia("(max-width: 768px)") : { matches: !1 };
function A(e) {
  if (v.has(e)) {
    const i = v.get(e);
    return v.delete(e), v.set(e, i), i;
  }
  if (v.size >= G) {
    const i = v.keys().next().value;
    v.delete(i);
  }
  const a = new Image();
  return a.src = e, v.set(e, a), a;
}
function H(e) {
  typeof requestIdleCallback == "function" ? requestIdleCallback(e, { timeout: 2e3 }) : setTimeout(e, 0);
}
let S = 0;
function V() {
  S = window.scrollY, document.body.style.position = "fixed", document.body.style.top = `-${S}px`, document.body.style.width = "100%", document.body.style.overflowY = "scroll";
}
function ee() {
  document.body.style.position = "", document.body.style.top = "", document.body.style.width = "", document.body.style.overflowY = "", window.scrollTo({
    top: S,
    left: 0,
    behavior: "instant"
  });
}
function F(e) {
  if (e.length < 2) return 0;
  const a = e[0], i = e[1];
  return Math.sqrt(
    Math.pow(i.screenX - a.screenX, 2) + Math.pow(i.screenY - a.screenY, 2)
  );
}
function K(e) {
  if (e.length < 2) return { x: e[0].screenX, y: e[0].screenY };
  const a = e[0], i = e[1];
  return {
    x: (a.screenX + i.screenX) / 2,
    y: (a.screenY + i.screenY) / 2
  };
}
function I(e) {
  e.style.transform = `scale(${t.scale}) translate(${t.x}px, ${t.y}px)`;
}
function R(e) {
  T || (T = !0, requestAnimationFrame(() => {
    I(e), T = !1;
  }));
}
function E(e, a = !1) {
  const i = !w(t.scale) || t.x !== 0 || t.y !== 0;
  t.scale = 1, t.x = 0, t.y = 0, a && i ? I(e) : (e.classList.add("lite-light-no-transform-transition"), I(e), requestAnimationFrame(() => {
    e.classList.remove("lite-light-no-transform-transition");
  }));
}
function te(e, a, i, o) {
  let h = !1;
  const m = () => {
    h || (h = !0, e.removeEventListener("transitionend", f), o());
  }, f = (p) => {
    p.target !== e || p.propertyName !== a || m();
  };
  e.addEventListener("transitionend", f), setTimeout(m, i + 50);
}
function L(e, a) {
  e.classList.toggle("lite-light-zooming", a);
}
function w(e) {
  return Math.abs(e - 1) < Q;
}
function ne(e = {}) {
  if (z) return;
  z = !0;
  const a = {
    imageSelector: e.imageSelector || "img[data-lightbox]",
    imageUrlAttribute: e.imageUrlAttribute || "data-lightbox",
    lightboxClass: e.lightboxClass || "lite-light",
    swipeThreshold: e.swipeThreshold || 50,
    fadeAnimationDuration: e.fadeAnimationDuration || 150,
    ...e
  };
  document.querySelector(`.${a.lightboxClass}`) || ie(a.lightboxClass);
  const i = document.querySelector(`.${a.lightboxClass}`), o = i.querySelector("img"), h = i.querySelector(".lite-light-prev"), m = i.querySelector(".lite-light-next"), f = i.querySelector(".lite-light-close");
  document.addEventListener("click", (p) => {
    if (p.target.tagName !== "IMG" || !p.target.hasAttribute(a.imageUrlAttribute)) return;
    const d = Array.from(document.querySelectorAll(a.imageSelector));
    let u = d.findIndex((n) => n === p.target), C = !1;
    function Z(n) {
      const s = (n - 1 + d.length) % d.length, l = (n + 1) % d.length;
      A(d[s].getAttribute(a.imageUrlAttribute)), A(d[l].getAttribute(a.imageUrlAttribute));
    }
    function q(n, s, l) {
      o.style.opacity = String(n), te(o, "opacity", s, l);
    }
    function Y(n) {
      if (C) return;
      C = !0, u = (n + d.length) % d.length;
      const s = d[u].getAttribute(a.imageUrlAttribute), l = A(s), r = a.fadeAnimationDuration, y = () => {
        H(() => Z(u)), C = !1;
      }, X = () => {
        o.style.opacity = "0", o.offsetWidth, q(1, r, y);
      }, k = () => {
        const x = () => {
          o.src = s, o.alt = d[u].alt || "", E(o), X();
        };
        l.decode ? l.decode().then(x).catch(x) : x();
      }, _ = () => q(0, r, k);
      w(t.scale) ? _() : (E(o, !0), o.addEventListener("transitionend", function x(W) {
        W.propertyName === "transform" && (o.removeEventListener("transitionend", x), _());
      }));
    }
    function g(n) {
      switch (n) {
        case "close":
          j();
          break;
        case "prev":
          Y(u - 1);
          break;
        case "next":
          Y(u + 1);
          break;
      }
    }
    function O(n) {
      const s = n.touches;
      if (s.length === 1) {
        const l = s[0];
        c.startX = l.screenX, c.startY = l.screenY, c.lastCenterX = l.screenX, c.lastCenterY = l.screenY, c.isZooming = !1, t.initialScale = t.scale, t.initialX = t.x, t.initialY = t.y;
      } else if (s.length === 2) {
        c.initialDistance = F(s);
        const l = K(s);
        c.lastCenterX = l.x, c.lastCenterY = l.y, t.initialScale = t.scale, t.initialX = t.x, t.initialY = t.y, c.isZooming = !0, L(o, !0);
      }
    }
    function U(n) {
      const s = n.touches;
      if (s.length === 2) {
        const l = F(s), r = K(s);
        if (c.initialDistance > 0) {
          const y = l / c.initialDistance;
          if (t.scale = Math.max(M, Math.min(J, t.initialScale * y)), w(t.scale))
            t.x = 0, t.y = 0;
          else {
            const X = r.x - c.lastCenterX, k = r.y - c.lastCenterY;
            t.scale > M && (t.x = t.initialX + X / t.scale, t.y = t.initialY + k / t.scale);
          }
          R(o), L(o, !0);
        }
        c.lastCenterX = r.x, c.lastCenterY = r.y, c.isZooming = !0;
      } else if (s.length === 1 && t.scale > M) {
        const l = s[0], r = l.screenX - c.lastCenterX, y = l.screenY - c.lastCenterY;
        t.x += r / t.scale, t.y += y / t.scale, R(o), L(o, !0), c.lastCenterX = l.screenX, c.lastCenterY = l.screenY, c.isZooming = !0;
      }
    }
    function N(n) {
      if (n.changedTouches.length === 1 && !c.isZooming && n.touches.length === 0 && w(t.scale)) {
        const s = n.changedTouches[0], l = s.screenX - c.startX, r = s.screenY - c.startY;
        Math.abs(l) > Math.abs(r) && Math.abs(l) > a.swipeThreshold && (Y(l > 0 ? u - 1 : u + 1), n.stopPropagation());
      }
      n.touches.length === 0 && (w(t.scale) && t.scale !== 1 && E(o, !0), c.isZooming = !1, c.initialDistance = 0, L(o, !1));
    }
    function P(n) {
      switch (n.key) {
        case "ArrowLeft":
          g("prev"), n.preventDefault();
          break;
        case "ArrowRight":
          g("next"), n.preventDefault();
          break;
        case "Escape":
          g("close"), n.preventDefault();
          break;
        case "Enter":
        case " ":
          document.activeElement === f ? (g("close"), n.preventDefault()) : document.activeElement === h && !D.matches ? (g("prev"), n.preventDefault()) : document.activeElement === m && !D.matches && (g("next"), n.preventDefault());
          break;
      }
    }
    function $(n) {
      if (n.key !== "Tab") return;
      const s = D.matches ? [f] : [f, h, m], l = s[0], r = s[s.length - 1];
      n.shiftKey ? (document.activeElement === l || !i.contains(document.activeElement)) && (n.preventDefault(), r.focus()) : (document.activeElement === r || !i.contains(document.activeElement)) && (n.preventDefault(), l.focus());
    }
    function b(n) {
      n.stopPropagation();
      const s = n.currentTarget;
      s === f ? g("close") : s === h ? g("prev") : s === m && g("next");
    }
    function B(n) {
      n.stopPropagation(), g("close");
    }
    function j() {
      i.classList.remove("lite-light-active"), E(o, !0), L(o, !1), o.style.opacity = "", ee(), document.removeEventListener("keydown", P), document.removeEventListener("keydown", $), i.removeEventListener("touchstart", O), i.removeEventListener("touchmove", U), i.removeEventListener("touchend", N), h.removeEventListener("click", b), m.removeEventListener("click", b), f.removeEventListener("click", b), i.removeEventListener("click", B);
    }
    i.style.setProperty("--ll-duration", `${a.fadeAnimationDuration}ms`), i.classList.add("lite-light-active"), A(d[u].getAttribute(a.imageUrlAttribute)), H(() => Z(u)), o.src = d[u].getAttribute(a.imageUrlAttribute), o.alt = d[u].alt || "", E(o), o.style.opacity = "0", o.offsetWidth, o.style.opacity = "1", V(), f.focus(), i.addEventListener("touchstart", O, { passive: !0 }), i.addEventListener("touchmove", U, { passive: !0 }), i.addEventListener("touchend", N, { passive: !0 }), document.addEventListener("keydown", P), document.addEventListener("keydown", $), h.addEventListener("click", b), m.addEventListener("click", b), f.addEventListener("click", b), i.addEventListener("click", B);
  });
}
function ie(e) {
  const a = `
    <div class="${e}" role="dialog" aria-modal="true" aria-label="Image lightbox">
      <div class="lite-light-prev lite-light-button" role="button" aria-label="Previous image" tabindex="0">
        <span class="lite-light-arrow lite-light-left"></span>
      </div>
      <img alt="" />
      <div class="lite-light-next lite-light-button" role="button" aria-label="Next image" tabindex="0">
        <span class="lite-light-arrow lite-light-right"></span>
      </div>
      <div class="lite-light-close lite-light-button" role="button" aria-label="Close lightbox" tabindex="0">
        <span class="lite-light-bar"></span>
        <span class="lite-light-bar"></span>
      </div>
    </div>
  `;
  document.body.insertAdjacentHTML("beforeend", a);
}
function ae(e) {
  ne(e);
}
export {
  ae as init,
  ne as initLiteLight
};
