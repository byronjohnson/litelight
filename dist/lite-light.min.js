const u = /* @__PURE__ */ new Map();
let n = {
  startX: 0,
  startY: 0,
  endX: 0,
  endY: 0,
  initialDistance: 0,
  isZooming: !1,
  lastCenterX: 0,
  lastCenterY: 0
}, t = {
  scale: 1,
  x: 0,
  y: 0,
  initialScale: 1,
  initialX: 0,
  initialY: 0
};
const B = 0.01, p = 1, H = 5;
let y = !1;
function f(e) {
  if (u.has(e)) {
    const s = u.get(e);
    return u.delete(e), u.set(e, s), s;
  }
  if (u.size >= 20) {
    const s = u.keys().next().value;
    u.delete(s);
  }
  const a = new Image();
  return a.src = e, u.set(e, a), a;
}
let x = 0;
function N() {
  x = window.scrollY, document.body.style.position = "fixed", document.body.style.top = `-${x}px`, document.body.style.width = "100%", document.body.style.overflowY = "scroll";
}
function R() {
  document.body.style.position = "", document.body.style.top = "", document.body.style.width = "", document.body.style.overflowY = "", window.scrollTo({
    top: x,
    left: 0,
    behavior: "instant"
  });
}
function I(e) {
  if (e.length < 2) return 0;
  const a = e[0], s = e[1];
  return Math.sqrt(
    Math.pow(s.screenX - a.screenX, 2) + Math.pow(s.screenY - a.screenY, 2)
  );
}
function Z(e) {
  if (e.length < 2) return { x: e[0].screenX, y: e[0].screenY };
  const a = e[0], s = e[1];
  return {
    x: (a.screenX + s.screenX) / 2,
    y: (a.screenY + s.screenY) / 2
  };
}
function q(e) {
  e.style.transform = `scale(${t.scale}) translate(${t.x}px, ${t.y}px)`, e.style.transformOrigin = "center center";
}
function O(e) {
  y || (y = !0, requestAnimationFrame(() => {
    q(e), y = !1;
  }));
}
function P(e) {
  t.scale = 1, t.x = 0, t.y = 0, q(e);
}
function U(e) {
  return Math.abs(e - 1) < B;
}
function j(e = {}) {
  const a = {
    imageSelector: e.imageSelector || "img[data-lightbox]",
    imageUrlAttribute: e.imageUrlAttribute || "data-lightbox",
    lightboxClass: e.lightboxClass || "lite-light",
    swipeThreshold: e.swipeThreshold || 50,
    fadeAnimationDuration: e.fadeAnimationDuration || 150,
    ...e
  };
  document.querySelector(`.${a.lightboxClass}`) || z(a.lightboxClass);
  const s = document.querySelector(`.${a.lightboxClass}`), c = s.querySelector("img"), L = s.querySelector(".lite-light-prev"), X = s.querySelector(".lite-light-next"), Y = s.querySelector(".lite-light-close");
  document.addEventListener("click", (v) => {
    if (v.target.tagName !== "IMG" || !v.target.hasAttribute(a.imageUrlAttribute)) return;
    const d = Array.from(document.querySelectorAll(a.imageSelector));
    let r = d.findIndex((i) => i === v.target);
    function A(i) {
      const l = (i - 1 + d.length) % d.length, o = (i + 1) % d.length;
      f(d[l].getAttribute(a.imageUrlAttribute)), f(d[o].getAttribute(a.imageUrlAttribute));
    }
    function m(i) {
      r = (i + d.length) % d.length;
      const l = d[r].getAttribute(a.imageUrlAttribute), o = f(l);
      c.classList.add("lite-light-fade-out"), c.addEventListener("animationend", function h() {
        const g = () => {
          c.src = l, P(c), c.classList.remove("lite-light-fade-out"), c.classList.add("lite-light-fade-in"), A(r), c.addEventListener("animationend", function() {
            c.classList.remove("lite-light-fade-in");
          }, { once: !0 });
        };
        o.decode ? o.decode().then(g).catch(g) : g(), c.removeEventListener("animationend", h);
      }, { once: !0 });
    }
    function C(i) {
      const l = i.touches;
      if (l.length === 1) {
        const o = l[0];
        n.startX = o.screenX, n.startY = o.screenY, n.lastCenterX = o.screenX, n.lastCenterY = o.screenY, n.isZooming = !1, t.initialScale = t.scale, t.initialX = t.x, t.initialY = t.y;
      } else if (l.length === 2) {
        n.initialDistance = I(l);
        const o = Z(l);
        n.lastCenterX = o.x, n.lastCenterY = o.y, t.initialScale = t.scale, t.initialX = t.x, t.initialY = t.y, n.isZooming = !0;
      }
    }
    function E(i) {
      const l = i.touches;
      if (l.length === 2) {
        const o = I(l), h = Z(l);
        if (n.initialDistance > 0) {
          const g = o / n.initialDistance;
          if (t.scale = Math.max(p, Math.min(H, t.initialScale * g)), U(t.scale))
            t.x = 0, t.y = 0;
          else {
            const _ = h.x - n.lastCenterX, $ = h.y - n.lastCenterY;
            t.scale > p && (t.x = t.initialX + _ / t.scale, t.y = t.initialY + $ / t.scale);
          }
          O(c);
        }
        n.lastCenterX = h.x, n.lastCenterY = h.y, n.isZooming = !0;
      } else if (l.length === 1 && t.scale > p) {
        const o = l[0], h = o.screenX - n.lastCenterX, g = o.screenY - n.lastCenterY;
        t.x += h / t.scale, t.y += g / t.scale, O(c), n.lastCenterX = o.screenX, n.lastCenterY = o.screenY, n.isZooming = !0;
      }
    }
    function w(i) {
      if (i.changedTouches.length === 1 && !n.isZooming && i.touches.length === 0 && U(t.scale)) {
        const l = i.changedTouches[0];
        n.endX = l.screenX, n.endY = l.screenY;
        const o = n.endX - n.startX, h = n.endY - n.startY;
        Math.abs(o) > Math.abs(h) && Math.abs(o) > a.swipeThreshold && (m(o > 0 ? r - 1 : r + 1), i.stopPropagation());
      }
      i.touches.length === 0 && (n.isZooming = !1, n.initialDistance = 0);
    }
    function M(i) {
      switch (i.key) {
        case "ArrowLeft":
          m(r - 1), i.preventDefault();
          break;
        case "ArrowRight":
          m(r + 1), i.preventDefault();
          break;
        case "Escape":
          b(), i.preventDefault();
          break;
      }
    }
    function k(i) {
      i.stopPropagation(), m(r - 1);
    }
    function S(i) {
      i.stopPropagation(), m(r + 1);
    }
    function T(i) {
      i.stopImmediatePropagation(), b();
    }
    function D(i) {
      i.stopImmediatePropagation(), b();
    }
    function b() {
      s.classList.remove("lite-light-active"), R(), document.removeEventListener("keydown", M), s.removeEventListener("touchstart", C), s.removeEventListener("touchmove", E), s.removeEventListener("touchend", w), L.removeEventListener("click", k), X.removeEventListener("click", S), Y.removeEventListener("click", T), s.removeEventListener("click", D);
    }
    s.classList.add("lite-light-active"), f(d[r].getAttribute(a.imageUrlAttribute)), A(r), c.src = d[r].getAttribute(a.imageUrlAttribute), P(c), c.classList.add("lite-light-fade-in"), N(), c.addEventListener("animationend", () => {
      c.classList.remove("lite-light-fade-in");
    }, { once: !0 }), s.addEventListener("touchstart", C, { passive: !0 }), s.addEventListener("touchmove", E, { passive: !0 }), s.addEventListener("touchend", w, { passive: !0 }), document.addEventListener("keydown", M), L.addEventListener("click", k), X.addEventListener("click", S), Y.addEventListener("click", T), s.addEventListener("click", D);
  });
}
function z(e) {
  const a = `
    <div class="${e}">
      <div class="lite-light-prev lite-light-button">
        <span class="lite-light-arrow lite-light-left"></span>
      </div>
      <img style="max-width: 90%; max-height: 90%;" />
      <div class="lite-light-next lite-light-button">
        <span class="lite-light-arrow lite-light-right"></span>
      </div>
      <div class="lite-light-close lite-light-button">
        <span class="lite-light-bar"></span>
        <span class="lite-light-bar"></span>
      </div>
    </div>
  `;
  document.body.insertAdjacentHTML("beforeend", a);
}
function F(e) {
  j(e);
}
export {
  F as init,
  j as initLiteLight
};
