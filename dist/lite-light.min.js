let j = !1;
const se = 20, v = /* @__PURE__ */ new Map();
let c = {
  startX: 0,
  startY: 0,
  initialDistance: 0,
  isZooming: !1,
  lastCenterX: 0,
  lastCenterY: 0
}, t = {
  scale: 1,
  x: 0,
  y: 0,
  initialScale: 1,
  initialX: 0,
  initialY: 0
};
const ae = 0.01, I = 1, oe = 5;
let S = !1;
const Z = typeof window < "u" ? window.matchMedia("(max-width: 768px)") : { matches: !1 };
function C(e) {
  if (v.has(e)) {
    const i = v.get(e);
    return v.delete(e), v.set(e, i), i;
  }
  if (v.size >= se) {
    const i = v.keys().next().value;
    v.delete(i);
  }
  const a = new Image();
  return a.src = e, v.set(e, a), a;
}
function G(e) {
  typeof requestIdleCallback == "function" ? requestIdleCallback(e, { timeout: 2e3 }) : setTimeout(e, 0);
}
let q = 0;
function le() {
  q = window.scrollY, document.body.style.position = "fixed", document.body.style.top = `-${q}px`, document.body.style.width = "100%", document.body.style.overflowY = "scroll";
}
function ce() {
  document.body.style.position = "", document.body.style.top = "", document.body.style.width = "", document.body.style.overflowY = "", window.scrollTo({
    top: q,
    left: 0,
    behavior: "instant"
  });
}
function Q(e) {
  if (e.length < 2) return 0;
  const a = e[0], i = e[1];
  return Math.sqrt(
    Math.pow(i.screenX - a.screenX, 2) + Math.pow(i.screenY - a.screenY, 2)
  );
}
function J(e) {
  if (e.length < 2) return { x: e[0].screenX, y: e[0].screenY };
  const a = e[0], i = e[1];
  return {
    x: (a.screenX + i.screenX) / 2,
    y: (a.screenY + i.screenY) / 2
  };
}
function O(e) {
  e.style.transform = `scale(${t.scale}) translate(${t.x}px, ${t.y}px)`;
}
function V(e) {
  S || (S = !0, requestAnimationFrame(() => {
    O(e), S = !1;
  }));
}
function x(e, a = !1) {
  const i = !E(t.scale) || t.x !== 0 || t.y !== 0;
  t.scale = 1, t.x = 0, t.y = 0, a && i ? O(e) : (e.classList.add("lite-light-no-transform-transition"), O(e), requestAnimationFrame(() => {
    e.classList.remove("lite-light-no-transform-transition");
  }));
}
function ee(e, a, i, s) {
  let g = !1;
  const m = () => {
    g || (g = !0, e.removeEventListener("transitionend", f), s());
  }, f = (p) => {
    p.target !== e || p.propertyName !== a || m();
  };
  e.addEventListener("transitionend", f), setTimeout(m, i + 50);
}
function L(e, a) {
  e.classList.toggle("lite-light-zooming", a);
}
function te(e, a) {
  const i = () => a(), s = () => i();
  e.complete && e.naturalWidth ? e.decode ? e.decode().then(i).catch(s) : i() : (e.onload = () => {
    e.decode ? e.decode().then(i).catch(s) : i();
  }, e.onerror = s);
}
function E(e) {
  return Math.abs(e - 1) < ae;
}
function re(e = {}) {
  if (j) return;
  j = !0;
  const a = {
    imageSelector: e.imageSelector || "img[data-lightbox]",
    imageUrlAttribute: e.imageUrlAttribute || "data-lightbox",
    lightboxClass: e.lightboxClass || "lite-light",
    swipeThreshold: e.swipeThreshold || 50,
    fadeAnimationDuration: e.fadeAnimationDuration || 150,
    ...e
  };
  document.querySelector(`.${a.lightboxClass}`) || de(a.lightboxClass);
  const i = document.querySelector(`.${a.lightboxClass}`), s = i.querySelector("img"), g = i.querySelector(".lite-light-prev"), m = i.querySelector(".lite-light-next"), f = i.querySelector(".lite-light-close");
  document.addEventListener("click", (p) => {
    if (p.target.tagName !== "IMG" || !p.target.hasAttribute(a.imageUrlAttribute)) return;
    const d = Array.from(document.querySelectorAll(a.imageSelector));
    let u = d.findIndex((n) => n === p.target), Y = !1;
    function U(n) {
      const o = (n - 1 + d.length) % d.length, l = (n + 1) % d.length;
      C(d[o].getAttribute(a.imageUrlAttribute)), C(d[l].getAttribute(a.imageUrlAttribute));
    }
    function w(n) {
      i.classList.toggle("lite-light-loading", n);
    }
    function X() {
      s.classList.remove("lite-light-entering", "lite-light-entered");
    }
    function N(n, o, l) {
      s.style.opacity = String(n), ee(s, "opacity", o, l);
    }
    function k(n) {
      if (Y) return;
      Y = !0, u = (n + d.length) % d.length;
      const o = d[u].getAttribute(a.imageUrlAttribute), l = C(o), r = a.fadeAnimationDuration, b = () => {
        G(() => U(u)), Y = !1;
      }, T = () => {
        s.style.opacity = "0", s.offsetWidth, N(1, r, b);
      }, D = () => {
        const A = !(l.complete && l.naturalWidth);
        A && w(!0), te(l, () => {
          s.src = o, s.alt = d[u].alt || "", x(s), A && w(!1), T();
        });
      }, K = () => N(0, r, D);
      E(t.scale) ? K() : (x(s, !0), s.addEventListener("transitionend", function A(ie) {
        ie.propertyName === "transform" && (s.removeEventListener("transitionend", A), K());
      }));
    }
    function h(n) {
      switch (n) {
        case "close":
          ne();
          break;
        case "prev":
          k(u - 1);
          break;
        case "next":
          k(u + 1);
          break;
      }
    }
    function P(n) {
      const o = n.touches;
      if (o.length === 1) {
        const l = o[0];
        c.startX = l.screenX, c.startY = l.screenY, c.lastCenterX = l.screenX, c.lastCenterY = l.screenY, c.isZooming = !1, t.initialScale = t.scale, t.initialX = t.x, t.initialY = t.y;
      } else if (o.length === 2) {
        c.initialDistance = Q(o);
        const l = J(o);
        c.lastCenterX = l.x, c.lastCenterY = l.y, t.initialScale = t.scale, t.initialX = t.x, t.initialY = t.y, c.isZooming = !0, L(s, !0);
      }
    }
    function $(n) {
      const o = n.touches;
      if (o.length === 2) {
        const l = Q(o), r = J(o);
        if (c.initialDistance > 0) {
          const b = l / c.initialDistance;
          if (t.scale = Math.max(I, Math.min(oe, t.initialScale * b)), E(t.scale))
            t.x = 0, t.y = 0;
          else {
            const T = r.x - c.lastCenterX, D = r.y - c.lastCenterY;
            t.scale > I && (t.x = t.initialX + T / t.scale, t.y = t.initialY + D / t.scale);
          }
          V(s), L(s, !0);
        }
        c.lastCenterX = r.x, c.lastCenterY = r.y, c.isZooming = !0;
      } else if (o.length === 1 && t.scale > I) {
        const l = o[0], r = l.screenX - c.lastCenterX, b = l.screenY - c.lastCenterY;
        t.x += r / t.scale, t.y += b / t.scale, V(s), L(s, !0), c.lastCenterX = l.screenX, c.lastCenterY = l.screenY, c.isZooming = !0;
      }
    }
    function B(n) {
      if (n.changedTouches.length === 1 && !c.isZooming && n.touches.length === 0 && E(t.scale)) {
        const o = n.changedTouches[0], l = o.screenX - c.startX, r = o.screenY - c.startY;
        Math.abs(l) > Math.abs(r) && Math.abs(l) > a.swipeThreshold && (k(l > 0 ? u - 1 : u + 1), n.stopPropagation());
      }
      n.touches.length === 0 && (E(t.scale) && t.scale !== 1 && x(s, !0), c.isZooming = !1, c.initialDistance = 0, L(s, !1));
    }
    function W(n) {
      switch (n.key) {
        case "ArrowLeft":
          h("prev"), n.preventDefault();
          break;
        case "ArrowRight":
          h("next"), n.preventDefault();
          break;
        case "Escape":
          h("close"), n.preventDefault();
          break;
        case "Enter":
        case " ":
          document.activeElement === f ? (h("close"), n.preventDefault()) : document.activeElement === g && !Z.matches ? (h("prev"), n.preventDefault()) : document.activeElement === m && !Z.matches && (h("next"), n.preventDefault());
          break;
      }
    }
    function _(n) {
      if (n.key !== "Tab") return;
      const o = Z.matches ? [f] : [f, g, m], l = o[0], r = o[o.length - 1];
      n.shiftKey ? (document.activeElement === l || !i.contains(document.activeElement)) && (n.preventDefault(), r.focus()) : (document.activeElement === r || !i.contains(document.activeElement)) && (n.preventDefault(), l.focus());
    }
    function y(n) {
      n.stopPropagation();
      const o = n.currentTarget;
      o === f ? h("close") : o === g ? h("prev") : o === m && h("next");
    }
    function z(n) {
      n.stopPropagation(), h("close");
    }
    function ne() {
      i.classList.remove("lite-light-active", "lite-light-loading"), x(s, !0), L(s, !1), X(), s.style.opacity = "", s.style.transform = "", ce(), document.removeEventListener("keydown", W), document.removeEventListener("keydown", _), i.removeEventListener("touchstart", P), i.removeEventListener("touchmove", $), i.removeEventListener("touchend", B), g.removeEventListener("click", y), m.removeEventListener("click", y), f.removeEventListener("click", y), i.removeEventListener("click", z);
    }
    const H = d[u].getAttribute(a.imageUrlAttribute), R = a.fadeAnimationDuration, M = C(H);
    i.style.setProperty("--ll-duration", `${R}ms`), i.classList.add("lite-light-active"), le(), f.focus(), G(() => U(u));
    const F = !(M.complete && M.naturalWidth);
    F && w(!0), te(M, () => {
      s.src = H, s.alt = d[u].alt || "", s.style.transform = "", X(), s.classList.add("lite-light-entering"), s.style.opacity = "0", s.offsetWidth, s.style.opacity = "1", s.classList.add("lite-light-entered"), F && w(!1), ee(s, "opacity", R, () => {
        X(), x(s);
      });
    }), i.addEventListener("touchstart", P, { passive: !0 }), i.addEventListener("touchmove", $, { passive: !0 }), i.addEventListener("touchend", B, { passive: !0 }), document.addEventListener("keydown", W), document.addEventListener("keydown", _), g.addEventListener("click", y), m.addEventListener("click", y), f.addEventListener("click", y), i.addEventListener("click", z);
  });
}
function de(e) {
  const a = `
    <div class="${e}" role="dialog" aria-modal="true" aria-label="Image lightbox">
      <div class="lite-light-prev lite-light-button" role="button" aria-label="Previous image" tabindex="0">
        <span class="lite-light-arrow lite-light-left"></span>
      </div>
      <img alt="" />
      <div class="lite-light-next lite-light-button" role="button" aria-label="Next image" tabindex="0">
        <span class="lite-light-arrow lite-light-right"></span>
      </div>
      <div class="lite-light-close lite-light-button" role="button" aria-label="Close lightbox" tabindex="0">
        <span class="lite-light-bar"></span>
        <span class="lite-light-bar"></span>
      </div>
    </div>
  `;
  document.body.insertAdjacentHTML("beforeend", a);
}
function ue(e) {
  re(e);
}
export {
  ue as init,
  re as initLiteLight
};
